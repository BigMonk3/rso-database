/**
 * enum for different user roles in the system
 * enum is for categorizeing and definining function access
 * for different types of users within the system
 */
public enum UserRole {
    SCIENTIST, SPACEAGENCYREP, POLICYMAKER, ADMINISTRATOR
}